import React from 'react';
import { useQuery } from '@apollo/client';

const Home = () => {
  return (
    <main>
     <div className='container'>
      <h3>To create a todo list, sign up or login and view your profile!</h3>
     </div>
    </main>
  );
};

export default Home;
